	/*
		 * List<WebElement> products =
		 * driver.findElements(By.cssSelector(".card-body button:last-of-type"));
		 * 
		 * System.out.println(products.size()); for (int i = 0; i < products.size();
		 * i++) { String value = products.get(i).getText(); System.out.println(value);
		 * if (value.equals("Add To Cart")) { products.get(i).click(); //
		 * Thread.sleep(2000); // products.get(1).click(); // Thread.sleep(2000); //
		 * products.get(2).click(); break; }
		 * 
		 * }
		 */